# Infrastructure as Code - Application

## Intructions for local setup

#### Create a variable
```
export TF_VAR_db_password="P@ssw0rd"
``` 

#### Run the command selecting the variables file
```
terraform plan -var-file="envs/test.tfvars"
```
